/**
 * Nodes that load external data into the graph — an image from disk, or a value from the
 * encrypted secret store (by key, so the secret itself is never wired or saved).
 */
package io.github.jaymcole.housegraph.graph.nodes.loader;
