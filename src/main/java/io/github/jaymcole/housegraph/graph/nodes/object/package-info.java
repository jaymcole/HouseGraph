/**
 * Nodes that operate on arbitrary objects — notably the object decomposer, which exposes
 * each readable property of whatever is wired in as its own typed output (dynamic ports).
 */
package io.github.jaymcole.housegraph.graph.nodes.object;
