/**
 * The node library: concrete {@link io.github.jaymcole.housegraph.graph.BaseNode}
 * subclasses, grouped one subpackage per category.
 * <p>
 * {@link io.github.jaymcole.housegraph.graph.NodeRegistry} discovers everything under
 * this package on the classpath, so a new node needs no registration — dropping a
 * class into a category subpackage is enough for it to appear in the Add-Node menu,
 * under that subpackage's name.
 * <p>
 * See {@code docs/architecture/nodes.md} for the add-a-node recipe.
 */
package io.github.jaymcole.housegraph.graph.nodes;
