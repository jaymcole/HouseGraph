/**
 * Example named-resource nodes (an echo resource and its listener) that demonstrate the
 * {@code ResourceRegistry} pattern — register/find and publish/subscribe by name — in
 * isolation from any external service.
 */
package io.github.jaymcole.housegraph.graph.nodes.resource;
