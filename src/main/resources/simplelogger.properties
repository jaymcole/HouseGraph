# SLF4J-simple config (used by JDA's logging). Keep it to warnings and errors so a
# connected bot doesn't spam the console with INFO chatter; problems still surface.
org.slf4j.simpleLogger.defaultLogLevel=warn
org.slf4j.simpleLogger.showThreadName=false
org.slf4j.simpleLogger.showLogName=false
