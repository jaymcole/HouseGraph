/**
 * Nodes that report information about the running HouseGraph engine itself — its version,
 * whether this process is a daemon or a desktop editor, where it keeps its files — rather than
 * about anything wired into the graph.
 */
package io.github.jaymcole.housegraph.graph.nodes.engine;
