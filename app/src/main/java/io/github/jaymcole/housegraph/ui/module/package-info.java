/**
 * The module surface of the desktop app: the picker that asks which module a new node should
 * reference.
 *
 * <p>Shells only. Every decision behind them — what is offered, what may be published, what a
 * publish means — lives in {@code modules/}, which is headless and tested; see
 * {@code docs/engine/ui-layer.md}.
 */
package io.github.jaymcole.housegraph.ui.module;
